A=123
B=$(A)
a:
	echo $(A) $(B) $(C)
A=456
C=789

# Makefile中都是终值
X=789
Y=$(X)abc
#Y=$(Y)def  #此处给Y赋值，则会认为Y没有被存在过，然后等号右边的引用就会出错，
#因为不能引用一个不存在的变量
Y:=$(Y)def #只受当前行和之前行影响不受后面的影响
b:
	echo $(X) $(Y)
X=456